import { Meta, Story, Canvas } from "@storybook/addon-docs";

<Meta title="Components/Interface/ProgressBar/Overview" />

<style>{`

`}</style>

# Progress bar

Basic progress bar component that listen to the itemIndex change.
When the itemIndex == itemsTotal the success message and the 'done button' are displayed.

Try it in All-stories / normal / canvas to understand how it works. Set Open = true there and start to play with itemIndex !

## Properties

```typescript
export type ProgressProps = {
  open: boolean;
  title: string;
  currentProgressIndex?: number;
  progressLength?: number;
  progressMessage?: string;
  successMessage?: string;
};
```

<Canvas>
  <Story id="components-interface-progressbar-all-stories--normal" />
</Canvas>

<Canvas>
  <Story id="components-interface-progressbar-all-stories--with_progress_msg" />
</Canvas>
